# MoonBit Language Pack

Canonical MoonParse language resources for `moonbit`.

The modules query marks public package exports and qualified `@alias.member`
value/type references for import-aware Workspace navigation.

The default `recommended` lint ruleset reports negative zero literals and
`TODO` comments. Negative zero has a safe Quick Fix that replaces `-0` with
`0`.
