# JSON Language Pack

Canonical MoonParse language resources for `json`.

The default `recommended` lint ruleset reports negative zero values and empty
object keys. Negative zero has a safe Quick Fix that replaces `-0` with `0`.
