{
  "name": "moonparse-lsp-server",
  "version": "0.1.0",
  "description": "MoonParse Language Server — LSP server powered by MoonParse GLR parser engine",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist/",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vscode-jsonrpc": "^8.2.0",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22"
  }
}
